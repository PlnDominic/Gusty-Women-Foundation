Impact stat card with count-up animation — the homepage "About strip" (Cohorts Run, Women Empowered, Partners).

```jsx
<StatCard value={250} suffix="+" label="Women Empowered" accent="purple" />
```

Three across on desktop, stacks on mobile. Rotate `accent` (purple / gold / magenta) across the row. Top accent bar instead of a left stripe.
