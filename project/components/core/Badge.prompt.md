Uppercase dot-prefixed status badge for programmes and articles.

```jsx
<Badge tone="open">Applications Open</Badge>
<Badge tone="soon">Coming Soon</Badge>
```

Tones: `open` (magenta), `soon` (gold), `closed` (grey), `info` (purple).
