Labelled form field — rounded (not pill), purple focus ring. Use for every form input across Apply / Contact / Newsletter.

```jsx
<Input label="Full name" placeholder="Ama Mensah" required />
<Input label="Why do you want to join?" multiline rows={5} />
```

Set `multiline` for essay/message fields. Required fields show a magenta asterisk.
